import { Typography } from "@mui/material";
import { PageView } from "./PageView";

export const Start = () => (
  <PageView title="Start page">
    <Typography>This is test application with Rick and Morty API</Typography>
  </PageView>
);
